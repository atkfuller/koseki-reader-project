# Main application venv: orchestration, preprocessing, PaddleOCR.
# Each other OCR engine gets its own venv (see venvs/) because ndlocr-lite and
# yomitoku pin conflicting versions of numpy/opencv/networkx/PyYAML.
paddlepaddle==3.3.1
paddleocr==3.7.0
opencv-contrib-python==4.10.0.84
pillow
numpy
pdf2image
